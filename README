Scummvm openelec addon.
-------------------------
Still in development stage. It compiles OK, but games don't start.

1. Copy scummvm directory to your OpenELEC.tv directory

2. Make addon with command:

PROJECT=project ARCH=architecture ./scripts/create_addon scummvm

for ION:
PROJECT=ION ARCH=x86_64 ./scripts/create_addon scummvm
